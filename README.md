Use `make <name>` to run a recipe.
Recipes write their outputs to the current working directory; build caches are kept in `.build/`.
Add future recipes in `recipes/<name>.ncl` and `modules/<name>.ncl`.
